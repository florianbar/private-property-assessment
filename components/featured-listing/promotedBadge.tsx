import Badge, { BADGE_SIZES } from '@/components/badge';

export default function FeaturedListingPromotedBadge() {
    return (
        <Badge size={BADGE_SIZES.small}>
            Promoted
        </Badge>
    );
}